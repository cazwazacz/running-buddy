<h1 id="heading">Running Buddy</h1>

<% @users.each do |user| %>
  <% if current_user && user.id != current_user.id %>
    <div>
      <%= user.username %> Run with me!
    </div>
  <% end %>
<% end %>
